-- core/palette.lua
PALETTE = {
    -- The "Certain Blue" for active cells
    ACTIVE = {0.2, 0.6, 1, 0.5},

    -- The "Blinker" (White) for empty hovering
    HOVER_EMPTY = {1, 1, 1, 0.2},

    -- The "Twist" (Brighter Blue) for hovering over active
    HOVER_ACTIVE = {0.4, 0.8, 1, 0.8},

    -- Reset / Text color
    DEFAULT = {1, 1, 1, 1}
}
