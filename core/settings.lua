-- core/settings.lua
SETTINGS = {
    -- World Geometry
    GRID_BUFFER_SIZE = 10240,
    CELL_SIZE = 2,

    -- Camera Movement
    CAMERA_SPEED = 10000, -- Pixels per second

    -- Controls
    KEYS = {
        UP    = "w",
        DOWN  = "s",
        LEFT  = "a",
        RIGHT = "d",
        ERASE = "lshift"
    }
}
